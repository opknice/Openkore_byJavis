# BamBoo Gepard V3 Server Configuration
# This server uses the Python MITM proxy (ro_packet_parser_v3_proxy_mitm.py)
# The proxy handles Gepard V3 encryption and forwards to real server

# Format:
# [Server Name]
# ip=127.0.0.1
# port=24657
# master_server_id=0
# server_type=0
# server_encoding=UTF8
# gamestate=0
# description=Description

# Note: OpenKore connects to the proxy on port 24657 (defined in Python proxy as OPENKORE_PORT)
# The proxy then forwards to the real server

[Bamboo Gepard]
ip=127.0.0.1
port=24657
master_server_id=0
server_type=0
server_encoding=UTF8
gamestate=0
description=Bamboo Gepard V3 Server (via MITM Proxy)
